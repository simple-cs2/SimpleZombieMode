# .github/workflows/build.yml
name: Build CI

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore SimpleZombieMode.csproj

      - name: Build
        run: dotnet build SimpleZombieMode.csproj --configuration Release --no-restore